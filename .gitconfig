[user]
	name = sharonrh
	email = sharon.rhe@gmail.com
[credential]
	helper = cache --timeout=36000
[push]
	default = simple
[alias]
	ac = !git add -A && git commit -m
	amend = commit --amend
	br = branch
	bye = branch -D
	cam = git add -A && git commit --amend
	cm = commit -m
	co = checkout
	d = diff
	dl = diff HEAD^
	df = "!git ll -1"
	fa = fetch --all
	fo = fetch origin
	l = log
	la = "!git config -l | grep alias | cut -c 7-"
	ll = log --decorate --pretty=format:'%C(yellow)%h%Creset%Cred%d%Creset %s %Cgreen(%cr) %C(bold blue)[%an]%Creset'
	lo = log --oneline --decorate
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --\n
	master = checkout master
	po = pull origin
	pop = stash pop
	pr = pull --rebase origin
	psf = push -f origin
	pso = push origin
	psk = push kumbokarno
	psu = "!git push --set-upstream origin `git rev-parse --abbrev-ref HEAD`"
	pt = push --tags
	ra = rebase --abort
	rc = rebase --continue
	ri = "!f() { git rebase -i HEAD~$1; }; f"
	rh = reset --hard
	s = status
	sh = stash save -u
	sl = stash list
	t = "!git tag `date +'v%Y%m%d%H%M%S'`"
	td = "!git tag -l | xargs git tag -d"
	tl = tag -l
	u = reset --soft HEAD~1
   	hist = "log --graph --pretty=format:'%Cred%h%Creset %d %s %Cgreen(%cr)%Creset %Cblue[%an]%Creset' --abbrev-commit --date=relative"
    	today = "log --stat --since='1 Day Ago' --graph --pretty=oneline --abbrev-commit --date=relative"
[core]
	editor = vim
[color]
	ui = true
